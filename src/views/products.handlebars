<div class="productDiv">
  <h1>Lista de Productos</h1>
  <div class="productBox"> 
    <ul class="ulProduct">
      {{#each products}}
      <li class="liProduct">
        <div class="productProperties"> 
          <h2>{{title}}</h2>
          <p>Precio: ${{price}}</p>
          <p>Categoría: {{category}}</p>
        </div>
        <div class="linksProduct">
          <button class="addToCart" href="#" data-product-id="{{_id}}">Agregar al carrito</button>
          <a href="/products/{{_id}}" class="product-details-link">Ver detalles</a>
        </div>
      </li>
      {{/each}}
    </ul>
  </div>

  <!-- Agregar controles de paginación -->
  <div class="pagination">
    {{#if hasPrevPage}}
    <a href="?page={{prevPage}}&limit={{limit}}">Página anterior</a>
    {{/if}}
    <span>Página {{page}} de {{totalPages}}</span>
    {{#if hasNextPage}}
    <a href="?page={{nextPage}}&limit={{limit}}">Página siguiente</a>
    {{/if}}
  </div>
</div>
<script type="module" src="/js/index.js"></script>

